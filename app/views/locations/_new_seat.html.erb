<h3>Book new seat here</h3>

<%= form_for(seat) do |f| %>
    <%= f.hidden_field :location_id %>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
